## 阶段一：架构设计 (全局一次性)

```markdown
# 阶段一：架构设计

我要实现 [需求/模块名称]。
请进入 **阶段一：架构设计**。

## 要求
1. **需求澄清：** 请先通过提问澄清需求细节（输入、输出、边界条件、异常场景）。
2. **子功能识别：** 识别该需求包含哪些子功能 (Sub-Features)，并列出。
3. **范围评估 (关键)：** 
   - 如果子功能过多 (预计文件 > 20 个)，建议拆分为多个 **SOP 周期**。
   - 如果范围可控，则进入下一步。
4. **结构提案：** 提出多文件、多模块的代码结构方案，说明：
   - 每个文件的职责
   - 文件间依赖关系
   - 建议的编写顺序
5. **生成文件清单：** 输出明确的文件列表（格式：`路径/文件名 - 职责简述 - 所属子功能`）。
6. **生成进度检查表：** 输出一个 Markdown 检查表，用于追踪每个子功能的完成状态。
7. **等待确认：** 等我确认结构合理性及范围后，再开始第一个文件的开发。

## 约束
- 不要一次性生成所有文件代码
- 每个文件将单独进入阶段二至四
- 请等待我的确认指令后再继续

# 阶段二：分层拆解

现在开始处理文件：`[文件路径/文件名]`。
请进入 **阶段二：分层拆解**。

## 要求
1. **文件定义：** 说明该文件的职责边界、输入、输出。
2. **递归细分：** 按以下层级细分：
   - 文件 → 函数/类 → 逻辑块
   - **每次只细分一级**，等我确认后再进入下一级
3. **终止条件：** 直到每个细分单元都能轻松映射为 **3-5 个元动作**。
4. **输出格式：** 使用树状结构展示细分结果。

## 约束
- 不要一次性细分到底
- 每级细分后等待我的确认
- 细分完成后提示我进入阶段三

# 阶段三：语义蓝图生成

拆解已完成。
请进入 **阶段三：语义蓝图生成**。

## 要求
1. **元动作映射：** 将最底层单元转换为元动作序列。
2. **AI 自检 (关键)：** 在输出前，请先对照 `docs/experience/00-seed/` 中的规则自我审查：
   - [ ] `I15 存储` 是否都配了 `F12 捕获`？
   - [ ] `I16 通信` 是否都配了 `timeout` 和 `F12 捕获`？
   - [ ] 是否有裸 `except` 或未处理的异常？
3. **生成元动作文档：** 创建不含实际代码的伪代码文档。
4. **输出审查报告：** 在蓝图下方列出 AI 自检报告表格。
5. **等待确认：** 等我审查确认后，**冻结蓝图**。

## 人类审查指引 (只看高风险)
| 优先级 | 元动作 | 审查要点 |
| :--- | :--- | :--- |
| 🔴 高 | `I15 存储`, `I16 通信` | 是否配了 `F12 捕获`？ |
| 🟡 中 | `F12 捕获` | 捕获后有没有日志/抛出？ |
| 🟢 低 | `I13 渲染`, `C6 条件` | 除非核心逻辑，否则跳过 |

## 约束
- 不要生成实际代码
- 自检不通过时先修正蓝图
- 冻结后不得随意偏离蓝图

# 阶段四：代码实现与验证

蓝图已冻结。
请进入 **阶段四：代码实现**。

## 要求
1. **严格按蓝图编码：** 严格按照元动作文档编写代码，不得随意偏离。
2. **元动作注释：** 代码中必须标注元动作注释。
3. **自动检查：** 代码需通过 ESLint/Flake8 检查。
4. **经验生成：** 如遇修正，请自动生成经验卡片。
5. **迭代提示：** 完成后，请提示我进入下一个文件的阶段二。

## 约束
- 不要一次性生成所有文件代码
- 每个文件完成后等待我的确认

# 错误处理与经验生成

代码出现以下问题：[错误信息]。

## 要求
1. **修正代码：** 请修正错误，确保符合 `.cursorrules` 和种子经验规则。
2. **生成经验卡片：** 请根据修正内容生成一张经验卡片，保存到 `docs/experience/01-runtime/`。
3. **更新自检规则：** 如果这是新类型的错误，请更新阶段三的 AI 自检清单。